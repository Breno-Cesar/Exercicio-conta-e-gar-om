#include <stdio.h>

int main() {
float area, base, altura;
printf("Digite a base do triângulo: ");
scanf("%f",&base);
printf("Digite altura do triângulo: ");
scanf("%f",&altura);
area=(base*altura)/2;
 printf("Area do triângulo=%.1f\n",area);
    return 0;
}
